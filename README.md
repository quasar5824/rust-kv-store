# Rust KV Store

A lightweight persistent key-value store implemented in Rust.

## Features
- Append-only log for durability
- Memory-mapped lookups via `HashMap`
- Simple JSON-based serialization

## How to run
```bash
cargo run
```